import {
  fetchAllTenants,
  fetchOneTenant,
  fetchTenantRegularizations
} from './tenant';

const Request = {
  fetchAllTenants,
  fetchOneTenant,
  fetchTenantRegularizations
};

export default Request;
